# Open Choice Reader backend

This package contains the FastAPI service used by Open Choice Reader. It is developed and distributed as part of the full repository rather than as a supported standalone library or command-line tool.

For installation, configuration, migration, and development instructions, use the main project README and the documentation in the repository's `docs` directory.
